import 'package:flutter/material.dart';

class WebMobileWidget extends StatelessWidget {
  const WebMobileWidget({super.key});

  @override
  Text build(BuildContext context) {
    return const Text("Page qui s'affiche que pour le mobile");
  }
}
